# Weather API 🌦️

Простой учебный проект с портала  
👉 [roadmap.sh — Weather API Wrapper Service](https://roadmap.sh/projects/weather-api-wrapper-service)

Это API-приложение с минимальным фронтендом, которое получает данные о погоде из стороннего сервиса, кеширует их и отдает через REST-интерфейс.

Проект создан в учебных целях для практики:
- работы с FastAPI
- асинхронных HTTP-запросов
- кеширования (Redis)
- контейнеризации (Docker)
- связки backend + frontend

---

## ⚙️ Стек

### Backend
- **FastAPI** — REST API
- **httpx** — асинхронные запросы к стороннему Weather API
- **Redis** — кеширование ответов
- **uv** — управление зависимостями

### Frontend
- **HTML**
- **CSS**
- **JavaScript**

### Infrastructure
- **Docker**
- **Nginx** — отдача фронтенда и проксирование запросов к API

---

## 🧠 Как это работает

1. Клиент отправляет запрос с названием города и периодом
2. Backend:
   - проверяет данные в Redis
   - если данных нет — запрашивает их у стороннего Weather API
   - сохраняет результат в кеш
3. API возвращает нормализованный JSON-ответ
4. Фронтенд отображает данные пользователю

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone https://github.com/dayanik/weather_api.git
cd weather_api
```

### 2. Настроить переменные окружения

Создайте файл `.env`:
```bash
cp example.env .env
```
[Сторонний сервис погоды](https://www.visualcrossing.com/). На нем получаем api-ключ.
```env
WEATHER_API_KEY=your_api_key
REDIS_HOST=redis
REDIS_PORT=6379
```

---

### 3. Запуск через Docker

```bash
docker-compose up -d --build
```

---

## 📡 Примеры запросов

### API запрос

```
GET /api?city=ufa&period=1
```

### Пример API-ответа

```json
{
  "days": [
    {
      "datetime": "2026-01-30",
      "tempmax": -4.4,
      "tempmin": -8,
      "temp": -6.3,
      "precip": 0.3,
      "windspeed": 39.6,
      "cloudcover": 92.9
    }
  ]
}
```

---

## 📄 Лицензия

Учебный проект. Используется свободно.
